package server

import "testing"

func TestSetRoutes(t *testing.T) {
	// Create a server
	// s := New("0.0.0.0:8888")
	//
	// Use s.routerEngine.Routes() to check the routes in setRoutes is set
	// for _, r := range s.routerEngine.Routes() {
	//   ...
	// }
}
